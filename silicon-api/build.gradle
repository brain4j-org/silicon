plugins {
    id 'java'
}

test {
    failOnNoDiscoveredTests = false
}

dependencies {
    testRuntimeOnly project(':silicon-cuda')
    testRuntimeOnly project(':silicon-opencl')
    testRuntimeOnly project(':silicon-metal')
}