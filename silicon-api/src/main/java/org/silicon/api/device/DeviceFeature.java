package org.silicon.api.device;

/**
 * Optional features that a device may support.
 * <p>
 * Used for capability checks before selecting data types or kernels.
 */
public enum DeviceFeature {
    FP16,
    FP64
}
