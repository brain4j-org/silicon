plugins {
    id 'java'
    alias libs.plugins.shadowJar
}

group = 'org.compute4j'
version = '1.0.0'

subprojects {
    version = rootProject.version
    group = rootProject.group

    apply plugin: 'java'

    repositories {
        mavenCentral()
    }
}